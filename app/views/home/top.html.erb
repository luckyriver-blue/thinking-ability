<h1>ホーム</h1>
<p>ようこそ<%= current_user.name %>さん!</p>
<h2><%= link_to "お題に回答して思考力を鍛える", problems_path %></h2>
<h2>あなたの投稿一覧</h2>
<ul>
  <% @posts.each do |post| %>
  <% problem = Problem.find(post.problem_id) %>
  <li>
    <%= link_to "#{problem.title}: #{post.content}", problem_post_path(problem, post) %>
  </li>
  <% end %>
</ul>